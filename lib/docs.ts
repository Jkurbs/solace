/** Public developer docs origin (API reference UI). */
export const DOCS_ORIGIN = 'https://docs.solace.fyi';

/** Path on the Next app that serves the API docs page. */
export const DOCS_API_APP_PATH = '/docs/api';

/** Canonical public URL for the Hermes market API docs. */
export const DOCS_API_URL = `${DOCS_ORIGIN}/api`;

/** JSON market-read endpoint (served from the main site / same app). */
export const HERMES_MARKET_API_PATH = '/api/hermes/market';
export const HERMES_MARKET_API_URL = `https://solace.fyi${HERMES_MARKET_API_PATH}`;
