/**
 * @deprecated Live exposure moved out of the table into TrustLivePanel so it
 * does not scroll away with sealed history. Kept as a re-export for safety.
 */
export { default } from './TrustLivePanel';
